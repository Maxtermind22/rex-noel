<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🦕 La Forêt du Rex Noël</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Segoe UI', 'Poppins', sans-serif;
            color: #e8dcc8;
            background: #0a0f0a;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* ===== FOND : FORÊT PRÉHISTORIQUE ===== */
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1542401886-65d6c61db217?w=1920&q=80') center/cover no-repeat;
            z-index: -2;
            filter: brightness(0.4) saturate(0.9) contrast(1.1);
        }

        .background::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(ellipse at center, rgba(10, 30, 10, 0.15) 0%, rgba(0, 5, 0, 0.75) 100%);
            z-index: -1;
        }

        /* ===== DINOSAURES EN FOND (VRAIS GIF) ===== */
        .dino-bg {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }

        .dino-bg img {
            position: absolute;
            opacity: 0.35;
            animation: dinoMove linear infinite;
        }

        /* Dino 1 : T-Rex (grand) */
        .dino-bg .dino-1 {
            width: 200px;
            bottom: 5%;
            left: -10%;
            animation-duration: 28s;
            opacity: 0.3;
        }

        /* Dino 2 : Brachiosaure (très grand) */
        .dino-bg .dino-2 {
            width: 280px;
            bottom: 0%;
            right: -10%;
            animation-duration: 35s;
            animation-delay: 2s;
            opacity: 0.2;
        }

        /* Dino 3 : Ptérosaure (qui vole en haut) */
        .dino-bg .dino-3 {
            width: 100px;
            top: 10%;
            left: -10%;
            animation-duration: 22s;
            animation-delay: 5s;
            opacity: 0.25;
            animation-name: dinoFly;
        }

        /* Dino 4 : Petit dinosaure (rapide) */
        .dino-bg .dino-4 {
            width: 120px;
            bottom: 2%;
            left: 30%;
            animation-duration: 20s;
            animation-delay: 8s;
            opacity: 0.25;
        }

        /* Dino 5 : T-Rex 2 */
        .dino-bg .dino-5 {
            width: 160px;
            bottom: 12%;
            right: 25%;
            animation-duration: 32s;
            animation-delay: 12s;
            opacity: 0.2;
        }

        @keyframes dinoMove {
            0% {
                transform: translateX(0) scaleX(1);
            }
            48% {
                transform: translateX(115vw) scaleX(1);
            }
            50% {
                transform: translateX(115vw) scaleX(-1);
            }
            98% {
                transform: translateX(0) scaleX(-1);
            }
            100% {
                transform: translateX(0) scaleX(1);
            }
        }

        @keyframes dinoFly {
            0% {
                transform: translate(0, 0) scaleX(1);
            }
            25% {
                transform: translate(35vw, -12vh) scaleX(1);
            }
            50% {
                transform: translate(70vw, 8vh) scaleX(1);
            }
            75% {
                transform: translate(100vw, -8vh) scaleX(-1);
            }
            100% {
                transform: translate(0, 0) scaleX(-1);
            }
        }

        /* ===== FLOCONS ===== */
        .snowflakes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
            overflow: hidden;
        }

        .snowflake {
            position: absolute;
            top: -20px;
            color: #fff;
            font-size: 1.2rem;
            animation: fall linear infinite;
            opacity: 0.5;
            text-shadow: 0 0 10px rgba(200, 220, 255, 0.15);
        }

        @keyframes fall {
            0% {
                transform: translateY(-20px) rotate(0deg);
                opacity: 0.5;
            }
            100% {
                transform: translateY(110vh) rotate(720deg);
                opacity: 0;
            }
        }

        /* ===== CONTENU ===== */
        .content {
            position: relative;
            z-index: 2;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* ===== HEADER ===== */
        .header {
            text-align: center;
            padding: 50px 20px 35px;
            background: rgba(8, 16, 8, 0.85);
            backdrop-filter: blur(8px);
            border-radius: 30px;
            margin-bottom: 40px;
            border: 2px solid rgba(200, 50, 30, 0.2);
            box-shadow: 0 10px 50px rgba(0, 0, 0, 0.8);
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦';
            position: absolute;
            top: 6px;
            left: 0;
            width: 100%;
            font-size: 0.7rem;
            color: #ffd700;
            opacity: 0.25;
            letter-spacing: 8px;
            animation: twinkle 3s ease-in-out infinite;
            pointer-events: none;
            white-space: nowrap;
        }

        .header::after {
            content: '✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦ ✦';
            position: absolute;
            bottom: 6px;
            left: 0;
            width: 100%;
            font-size: 0.7rem;
            color: #ffd700;
            opacity: 0.15;
            letter-spacing: 8px;
            animation: twinkle 4s ease-in-out infinite reverse;
            pointer-events: none;
            white-space: nowrap;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.1; }
            50% { opacity: 0.5; }
        }

        .header h1 {
            font-family: 'Fredoka One', 'Impact', cursive;
            font-size: 3.8rem;
            color: #f5e6d3;
            text-shadow: 0 4px 30px rgba(200, 50, 20, 0.4);
            letter-spacing: 4px;
            position: relative;
            z-index: 1;
        }

        .header h1 .rex {
            color: #e64a2a;
            text-shadow: 0 0 40px rgba(230, 74, 42, 0.3);
        }

        .header h1 .noel {
            color: #ffd700;
            text-shadow: 0 0 40px rgba(255, 215, 0, 0.2);
        }

        .header .slogan {
            font-size: 1.1rem;
            color: #b8a88a;
            margin-top: 12px;
            font-style: italic;
            background: rgba(0, 0, 0, 0.4);
            display: inline-block;
            padding: 8px 30px;
            border-radius: 50px;
            backdrop-filter: blur(4px);
            border: 1px solid rgba(200, 100, 50, 0.08);
            position: relative;
            z-index: 1;
        }

        .header .tree {
            position: absolute;
            right: 20px;
            bottom: 15px;
            font-size: 2.8rem;
            opacity: 0.25;
            animation: treeGlow 3s ease-in-out infinite;
            z-index: 1;
        }

        @keyframes treeGlow {
            0%, 100% { opacity: 0.15; transform: scale(1); }
            50% { opacity: 0.4; transform: scale(1.1); }
        }

        /* ===== NAVIGATION ===== */
        .navbar {
            position: sticky;
            top: 0;
            z-index: 100;
            background: rgba(8, 16, 8, 0.92);
            backdrop-filter: blur(16px);
            padding: 12px 20px;
            border-radius: 60px;
            margin: 20px auto 40px;
            max-width: 650px;
            display: flex;
            justify-content: center;
            gap: 25px;
            border: 1px solid rgba(200, 100, 50, 0.12);
            box-shadow: 0 8px 40px rgba(0, 0, 0, 0.6);
        }

        .navbar a {
            color: #d4c8b0;
            text-decoration: none;
            font-size: 1.1rem;
            font-weight: 600;
            padding: 6px 18px;
            border-radius: 40px;
            transition: all 0.3s ease;
            letter-spacing: 1px;
            position: relative;
        }

        .navbar a::after {
            content: '';
            position: absolute;
            bottom: 2px;
            left: 50%;
            width: 0;
            height: 2px;
            background: #e64a2a;
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        .navbar a:hover::after {
            width: 60%;
        }

        .navbar a:hover {
            color: #fff;
            transform: scale(1.05);
        }

        /* ===== CATÉGORIES ===== */
        .category {
            background: rgba(8, 16, 8, 0.82);
            backdrop-filter: blur(8px);
            border-radius: 30px;
            padding: 28px 22px 36px;
            margin-bottom: 45px;
            border: 1px solid rgba(200, 150, 80, 0.06);
            box-shadow: 0 12px 50px rgba(0, 0, 0, 0.6);
        }

        .category h2 {
            font-family: 'Fredoka One', 'Impact', cursive;
            font-size: 2.2rem;
            color: #f0d8b0;
            text-shadow: 0 2px 15px rgba(200, 80, 30, 0.15);
            border-bottom: 2px solid rgba(200, 100, 50, 0.08);
            padding-bottom: 12px;
            margin-bottom: 22px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .category h2 .emoji-cat {
            font-size: 1.8rem;
        }

        .category h2 .light {
            margin-left: auto;
            font-size: 1.2rem;
            animation: twinkle 2s ease-in-out infinite;
            opacity: 0.4;
        }

        /* ===== GRILLE ===== */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 24px;
        }

        /* ===== CARTE ===== */
        .card {
            background: rgba(18, 14, 8, 0.85);
            backdrop-filter: blur(4px);
            border-radius: 20px;
            padding: 16px 14px 20px;
            text-align: center;
            border: 1px solid rgba(200, 150, 80, 0.05);
            transition: all 0.4s ease;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.6s ease forwards;
            box-shadow: 0 4px 25px rgba(0, 0, 0, 0.4);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #ffd700, #e64a2a, #ffd700, transparent);
            opacity: 0.15;
            transition: all 0.5s ease;
        }

        .card:hover::before {
            opacity: 0.6;
            height: 3px;
        }

        .card:nth-child(1) { animation-delay: 0.1s; }
        .card:nth-child(2) { animation-delay: 0.2s; }
        .card:nth-child(3) { animation-delay: 0.3s; }
        .card:nth-child(4) { animation-delay: 0.4s; }

        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(30px) scale(0.95);
            }
            100% {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .card:hover {
            transform: translateY(-10px) scale(1.02);
            border-color: rgba(200, 100, 50, 0.12);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.8);
            background: rgba(28, 20, 10, 0.92);
        }

        .card .image-placeholder {
            width: 100%;
            height: 140px;
            background: radial-gradient(ellipse at center, #1a2a1a, #0a120a);
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            margin-bottom: 12px;
            border: 1px solid rgba(200, 150, 80, 0.05);
            transition: all 0.3s ease;
        }

        .card:hover .image-placeholder {
            border-color: rgba(200, 100, 50, 0.12);
            background: radial-gradient(ellipse at center, #2a3a2a, #121a12);
        }

        .card .product-name {
            font-size: 1.1rem;
            font-weight: 700;
            color: #f0e0c8;
            margin-bottom: 4px;
        }

        .card .product-desc {
            font-size: 0.82rem;
            color: #a89878;
            margin-bottom: 8px;
            line-height: 1.3;
            min-height: 36px;
        }

        .card .product-price {
            font-size: 1.4rem;
            font-weight: 800;
            color: #ff6b4a;
            background: rgba(0, 0, 0, 0.3);
            display: inline-block;
            padding: 4px 20px;
            border-radius: 40px;
            border: 1px solid rgba(255, 100, 50, 0.05);
        }

        .card .product-price small {
            font-size: 0.8rem;
            font-weight: 400;
            color: #a89878;
        }

        /* ===== FOOTER ===== */
        footer {
            text-align: center;
            color: #3a4a3a;
            padding: 25px 0 10px;
            font-size: 0.8rem;
            opacity: 0.35;
            border-top: 1px solid rgba(200, 150, 80, 0.03);
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .header h1 { font-size: 2.4rem; }
            .header .tree { display: none; }
            .navbar { flex-wrap: wrap; gap: 8px; border-radius: 30px; padding: 10px 14px; max-width: 100%; }
            .navbar a { font-size: 0.95rem; padding: 5px 12px; }
            .category h2 { font-size: 1.8rem; }
            .grid { grid-template-columns: 1fr 1fr; gap: 16px; }
            .header::before, .header::after { display: none; }
            .dino-bg img { opacity: 0.15 !important; }
        }

        @media (max-width: 480px) {
            .grid { grid-template-columns: 1fr; }
            .header h1 { font-size: 1.8rem; }
            .header .slogan { font-size: 0.8rem; padding: 5px 14px; }
            .category { padding: 18px 14px 24px; }
            .card .image-placeholder { height: 110px; font-size: 2.4rem; }
            .dino-bg img { display: none; }
        }

        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #0a120a; }
        ::-webkit-scrollbar-thumb { background: #2a1a1a; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #3a2a2a; }
    </style>
</head>
<body>

    <!-- ===== FOND : FORÊT PRÉHISTORIQUE ===== -->
    <div class="background"></div>

    <!-- ===== DINOSAURES EN FOND (VRAIS GIF) ===== -->
    <div class="dino-bg">
        <!-- T-Rex -->
        <img class="dino-1" src="https://media.giphy.com/media/3oKIPnAiaMCws8nOsE/giphy.gif" alt="T-Rex">
        <!-- Brachiosaure -->
        <img class="dino-2" src="https://media.giphy.com/media/l41lMqnFf6r6pCb8A/giphy.gif" alt="Brachiosaure">
        <!-- Ptérosaure -->
        <img class="dino-3" src="https://media.giphy.com/media/VzGKxbL8RRkUw/giphy.gif" alt="Ptérosaure">
        <!-- Petit dinosaure -->
        <img class="dino-4" src="https://media.giphy.com/media/xUA7b3ZP9T5lU/giphy.gif" alt="Petit dinosaure">
        <!-- T-Rex 2 -->
        <img class="dino-5" src="https://media.giphy.com/media/3oKIPnAiaMCws8nOsE/giphy.gif" alt="T-Rex 2">
    </div>

    <!-- ===== FLOCONS ===== -->
    <div class="snowflakes" id="snowflakes"></div>

    <!-- ===== CONTENU ===== -->
    <div class="content">

        <header class="header">
            <div class="tree">🎄</div>
            <h1>
                🦕 La Forêt du <span class="rex">Rex</span> <span class="noel">Noël</span> 🦖
            </h1>
            <div class="slogan">
                ❄️ Le catalogue préhistorique des cadeaux de Noël — édition 2026
            </div>
        </header>

        <nav class="navbar">
            <a href="#librairie">📚 Librairie</a>
            <a href="#gaming">🎮 Gaming</a>
            <a href="#loisirs">🧶 Loisirs créatifs</a>
        </nav>

        <!-- LIBRAIRIE -->
        <section class="category" id="librairie">
            <h2>
                <span class="emoji-cat">📚</span> Librairie
                <span class="light">✦</span>
            </h2>
            <div class="grid">
                <div class="card">
                    <div class="image-placeholder">📘</div>
                    <div class="product-name">Jungle Juice Tome 5</div>
                    <div class="product-desc">Webtoon coréen — l'aventure continue !</div>
                    <div class="product-price">14.99 <small>€</small></div>
                </div>
                <div class="card">
                    <div class="image-placeholder">📕</div>
                    <div class="product-name">Demon Slave Tome 18</div>
                    <div class="product-desc">Manga de Takahiro — à partir du tome 18</div>
                    <div class="product-price">7.30 <small>€</small></div>
                </div>
            </div>
        </section>

        <!-- GAMING -->
        <section class="category" id="gaming">
            <h2>
                <span class="emoji-cat">🎮</span> Gaming
                <span class="light">✦</span>
            </h2>
            <div class="grid">
                <div class="card">
                    <div class="image-placeholder">🎯</div>
                    <div class="product-name">Echoes of Aincrad</div>
                    <div class="product-desc">Jeux vidéo PS5 — nouvelle aventure épique</div>
                    <div class="product-price">69.99 <small>€</small></div>
                </div>
                <div class="card">
                    <div class="image-placeholder">🏎️</div>
                    <div class="product-name">GTA VI</div>
                    <div class="product-desc">Jeux vidéo PS5 — le retour de la légende</div>
                    <div class="product-price">79.99 <small>€</small></div>
                </div>
            </div>
        </section>

        <!-- LOISIRS CRÉATIFS -->
        <section class="category" id="loisirs">
            <h2>
                <span class="emoji-cat">🧶</span> Loisirs créatifs
                <span class="light">✦</span>
            </h2>
            <div class="grid">
                <div class="card">
                    <div class="image-placeholder">🏛️</div>
                    <div class="product-name">LEGO Architecture Notre-Dame</div>
                    <div class="product-desc">Set LEGO 21061 — La cathédrale en briques</div>
                    <div class="product-price">160.99 <small>€</small></div>
                </div>
                <div class="card">
                    <div class="image-placeholder">🦖</div>
                    <div class="product-name">Fossiles T-Rex LEGO</div>
                    <div class="product-desc">Set LEGO — Le Tyrannosaurus rex à monter</div>
                    <div class="product-price">249.99 <small>€</small></div>
                </div>
                <div class="card">
                    <div class="image-placeholder">🎨</div>
                    <div class="product-name">Puzzle Totoro</div>
                    <div class="product-desc">Puzzle vitrail 126 pièces — My Neighbor Totoro</div>
                    <div class="product-price">30.00 <small>€</small></div>
                </div>
            </div>
        </section>

        <footer>
            🦕 La Forêt du Rex Noël • Catalogue cadeaux 2026 • Fait avec ❤️
        </footer>

    </div>

    <script>
        (function() {
            const container = document.getElementById('snowflakes');
            const flakes = ['❄', '❅', '❆', '✦', '•'];
            for (let i = 0; i < 55; i++) {
                const flake = document.createElement('div');
                flake.className = 'snowflake';
                flake.textContent = flakes[Math.floor(Math.random() * flakes.length)];
                flake.style.left = Math.random() * 100 + '%';
                flake.style.fontSize = (0.5 + Math.random() * 1.8) + 'rem';
                flake.style.opacity = 0.15 + Math.random() * 0.4;
                flake.style.animationDuration = (10 + Math.random() * 18) + 's';
                flake.style.animationDelay = (Math.random() * 20) + 's';
                container.appendChild(flake);
            }
        })();
    </script>

</body>
</html>